/* 54. Define dos variables, `numero1` y `numero2`, y realiza una comparación para
determinar cuál de los dos números es mayor. Luego, muestra el número mayor en la
consola. */

let numero1 = 32;
let numero2 = 17;

if (numero1 > numero2) {
  console.log(numero1);
} else {
  console.log(numero2);
}
